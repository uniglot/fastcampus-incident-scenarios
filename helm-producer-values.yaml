common:
  nameOverride: fastcampus-producer
  fullnameOverride: fastcampus-producer

serviceAccount:
  create: true

workload:
  kind: Deployment
  replicaCount: 3
  image:
    ecr:
      enabled: true
      repository: fastcampus-app-image
  resources:
    requests:
      cpu: 300m
      memory: 256Mi
    limits:
      cpu: 600m
      memory: 512Mi
  containerPort: 5000
  scriptName: producer.py
  probes:
    livenessProbe:
      path: /
      conditions:
        initialDelaySeconds: 20
        timeoutSeconds: 15
        periodSeconds: 10
        successThreshold: 1
        failureThreshold: 3
    readinessProbe:
      path: /
      conditions:
        initialDelaySeconds: 20
        timeoutSeconds: 15
        periodSeconds: 10
        successThreshold: 1
        failureThreshold: 3

autoscaling:
  enabled: false

service:
  type: NodePort
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
      name: http

ingress:
  enabled: true
  className: alb
  hosts:
    - paths:
        - path: /
          pathType: Prefix
